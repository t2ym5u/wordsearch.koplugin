## What does this change

## Testing
- [ ] `busted spec/` passes locally (if this plugin has a `spec/` suite)
- [ ] Tested manually in KOReader (device or emulator)

## Version bump
- [ ] Left `_meta.lua`'s version alone (the maintainer bumps it on release, via the koreader-plugins monorepo's `scripts/bump_versions.sh`) unless asked otherwise
